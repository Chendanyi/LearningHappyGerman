// Copy this file to Secrets.xcconfig (same folder) and add your key.
// Secrets.xcconfig is gitignored — do not commit it.
// The app merges GOOGLE_AI_API_KEY into Info.plist via AppInfoAdditions.plist (see README).

GOOGLE_AI_API_KEY = YOUR_API_KEY_HERE
