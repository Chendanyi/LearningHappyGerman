import Foundation
import SnapshotTesting

/// Reserved for future visual regression tests (Lobby symmetry, classroom layouts).
/// Wire snapshots from a test target that links this library or add an Xcode SPM dependency on swift-snapshot-testing.
public enum SnapshotTestingBootstrap {
    public static let libraryLinked = true
}
